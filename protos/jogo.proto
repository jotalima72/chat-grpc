syntax = "proto3"; //Specify proto3 version.

service Jogo {
    rpc join(Message) returns (stream Message){}
    rpc send(Message) returns (Message){}
    rpc exit(Message) returns (Message){}
}

message Message {
    string user = 1;
    string palavra = 2;
}
